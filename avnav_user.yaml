#mapproxy user config
#never delete the avnav_base include as it defines the necessary services
base: 
  - avnav_base.yaml
  - bsh.yaml
  - osea.yaml
  - noaa.yaml
  - wsv.yaml

#just define your sources, layers and caches here or add a new include to the list above

